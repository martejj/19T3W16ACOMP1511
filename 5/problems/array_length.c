#include <stdio.h>

#define HAND_SIZE 10

int array_length(int array[], int length) {
    return length;
}

/* 
int add(int a, int b) {
    return a + b;
}*/

int main(void) {
    int length = 5;
    int input[ARRAY_LENGTH] = {-1, 0, 1, 2, 3};
    

    array_length(input, length);
    
    return 0;
}

